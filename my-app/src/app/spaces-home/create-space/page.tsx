import UploadImage from "../components/UploadImage";

const CreateSpacesPage = async () => {
  return (
    <div style={{ marginTop: "6%" }}>
      <h2 style={{ textAlign: "center" }}>Create a space</h2>
      <UploadImage />
    </div>
  );
};

export default CreateSpacesPage;
